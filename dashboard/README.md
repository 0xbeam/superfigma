# Dashboard

This folder tracks the Sanctuary dashboard as a separate concern from the main monorepo.

- Preferred branch for shared notes and integration docs: `project/dashboard-fork`
- Dedicated git remote: `dashboard-fork`

The actual dashboard codebase should stay in its own repo history. This monorepo keeps only the organizational layer around it so the main Sanctuary Parc repo stays clean.
